---
description: 提取 Active Directory 环境中的子网
---

# 提取子网

```
$ nxc ldap <ip> -u <user> -p <pass> -M get-network
$ nxc ldap <ip> -u <user> -p <pass> -M get-network -o ONLY_HOSTS=true
$ nxc ldap <ip> -u <user> -p <pass> -M get-network -o ALL=true
```
